mydb = {
  url = "jdbc:postgresql://postgres-service.default.svc.cluster.local:5432/my-postgresql"
  user = "postgres"
  password = "duxWygN3Sc"  // replace with the actual password retrieved from the secret
  driver = "org.postgresql.Driver"
  connectionPool = "HikariCP"
}